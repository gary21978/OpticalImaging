import torch

class Recipe:
    # focus setting
    Focus = torch.tensor([0])  # Unit: nm
    FocusRange = torch.tensor([-200, 0, 200])

    # Developing parameters
    def __init__(self):
        pass
        
